fastapi==0.111.0
uvicorn==0.30.1
python-dotenv==1.0.0
# Pose landmarks AND the person mask come from one model; see README "Why one
# model". Pinned exactly, because the landmark indices below are a model
# contract, not a library API.
mediapipe==0.10.35
opencv-python-headless==5.0.0.93
pillow==12.3.0
numpy==2.5.3
